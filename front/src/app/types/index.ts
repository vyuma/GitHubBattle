export interface Community {
    id: number;
    name: string;
    description: string;
    commitCount: number;
}

export interface User {
    id: number;
    username: string;
    commitCount: number;
}
