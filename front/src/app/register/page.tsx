const RegisterPage = () => {
    return (
        <div>
            <h1>新規登録</h1>
            <input type="text" placeholder="メールアドレス" />
            <input type="text" name="" id="" placeholder="githubのユーザー名" />
            <button>登録</button>
        </div>
    );
};

export default RegisterPage;
