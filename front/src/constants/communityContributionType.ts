export interface communityContributionType{
    rank: number;
    community_id: string;
    total_contributions: string;
    all_members_committed:boolean;
    community_name: string;
    created_at: string;
}